{
  "name": "shijianbu-game",
  "version": "1.0.0",
  "description": "时间步游戏 - 账号系统、排位赛、成就系统",
  "scripts": {
    "dev": "python -m http.server 8080",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
